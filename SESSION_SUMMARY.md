# RaceOpt — สรุปงานที่ทำในเซสชันนี้ (2026-08-27)

ภาพรวมงานที่ทำในเซสชันนี้ เรียงตามลำดับ ขอบเขต: `app.py`, `data_pipeline.py`,
`race_simulator.py`, `strategy_optimizer.py` และไฟล์ทั้ง 4 ใน `templates/`
(`model.pkl` / `train_model_advanced.py` ขึ้นสถานะ modified ใน git status มาตั้งแต่ก่อนเซสชันนี้
— ไม่ได้แตะในเซสชันนี้)

---

## 1. ทำให้ข้อมูลบนเว็บเป็นข้อมูลจริง (ไม่ hardcode)

เซสชันก่อนหน้ามีข้อจำกัดให้แก้เฉพาะ `templates/` เท่านั้น แต่คำขอของเซสชันนี้
("แสดงข้อมูลจริงๆ ครบฟีเจอร์") ต้องแก้ backend ด้วย จึงขออนุญาตผู้ใช้ยกเลิกข้อจำกัดนั้น
อย่างชัดเจนก่อนเริ่มแก้ `app.py` และไฟล์อื่นๆ

- **หน้า Model Report** — ตารางตรวจสอบโมเดลเดิมเป็นตัวเลขที่ hardcode ไว้ในโค้ด
  (`TRAIN_COMBO_STATS`) ตอนนี้เปลี่ยนเป็นคำนวณจริงตอน app เริ่มทำงาน โดยสแกนหา
  `cache/*.pkl` ที่มีอยู่แล้ว แล้วรันโมเดลจริงกับ lap data จริง เหมือนที่หน้า Analysis
  ทำอยู่แล้วสำหรับนักแข่งคนเดียว
- **กริดใน Strategy Lab** — เดิมใช้รายชื่อนักแข่ง 20 คนที่ hardcode ไว้จากฤดูกาล 2023
  ไม่ว่าจะเลือกสนาม/ปีไหนก็แข่งกับกริดชุดเดิม ตอนนี้ดึงรายชื่อนักแข่งจริงของสนามที่เลือก
  (เช่น เลือกสนามปี 2025 ก็จะแข่งกับกริดจริงของปี 2025 รวมถึงนักแข่งอย่าง BEA/BOR/DOO/HAD/LAW
  ที่ไม่มีอยู่ในรายชื่อเดิม) พร้อม pace ต่อคนที่คำนวณจาก lap cache จริงถ้ามีข้อมูล
- **แก้บั๊กที่ซ่อนอยู่**: โค้ดเดิมเทียบเวลาจริงของนักแข่งโดยเทียบชื่อสนาม (string) กับ
  round number (int) ซึ่งไม่มีทางตรงกันได้เลย ทำให้ "เวลาจริง" อ้างอิงใน Strategy Lab
  กลายเป็นค่าประมาณ fallback อยู่ตลอดโดยไม่มีใครรู้ ตอนนี้แก้ให้เทียบด้วย round number
  สอดคล้องกันแล้ว
- **รองรับ 2-stop ใน Strategy Lab** — เดิม grid search ในหน้า Analysis หากลยุทธ์ 2-stop
  ที่เร็วกว่าได้ แต่ Strategy Lab จำลองได้แค่ 1-stop ทำให้ลองกลยุทธ์ที่ Analysis บอกว่าดีที่สุด
  ไม่ได้จริง จึงเพิ่มฟิลด์รอบพิทครั้งที่ 2 กับยาง stint ที่ 3 เข้าไปทั้งฟอร์มและ backend

## 2. ตรวจสอบ UI/UX (ผ่าน skill ui-ux-pro-max)

ตรวจฟิลด์ 2-stop ที่เพิ่มใหม่ตาม checklist ของ skill แล้วพบปัญหา "overwhelm upfront"
(ยัดข้อมูลที่ไม่เกี่ยวข้องให้ผู้ใช้เห็นตั้งแต่แรก) — ฟิลด์รอบพิทครั้งที่ 2/ยาง stint 3
โชว์อยู่ตลอดแม้เลือก 1 stop จึงแก้ด้วย vanilla JS เล็กๆ ให้โชว์เฉพาะตอนเลือก "2 stops"
เท่านั้น (ไม่ได้เพิ่ม framework ใดๆ)

## 3. ปรับดีไซน์ใหม่ทั้งเว็บ — จาก motorsport engineering สู่สไตล์ "F1 broadcast สมัยใหม่"

โจทย์: ทำให้เว็บดูรวดเร็ว ดุดัน ทันสมัย เหมือนกราฟิก timing/broadcast ของ F1 จริงๆ
โดยห้ามคัดลอกโลโก้ เครื่องหมายการค้า หรือหน้าตาของ F1 โดยตรง

- **ตัวอักษร**: เพิ่มฟอนต์ IBM Plex Sans Condensed (ตระกูลเดียวกับ Plex Mono/Sans Thai
  ที่ใช้อยู่แล้ว) สำหรับหัวข้อ ตัวเลขสำคัญขนาดใหญ่ และปุ่มต่างๆ
- **มุมเฉียง**: การ์ดและปุ่มมีมุมตัดเฉียงแบบบางๆ (`clip-path`) โดยพื้นหลังมุมเฉียงของปุ่ม
  แยกไว้บน `::before` โดยเฉพาะ เพื่อให้กรอบ focus ตอนกด Tab ยังเป็นสี่เหลี่ยมเต็มรูป
  ไม่โดนตัดไปด้วย (ทดสอบจริงในเบราว์เซอร์แล้วว่ากรอบ focus ยังเห็นชัดเจน)
- **ลวดลายที่ได้แรงบันดาลใจจากสนามแข่ง (สร้างด้วย CSS ล้วนๆ ไม่ใช้รูปภาพ/โลโก้)**:
  เส้นแบ่ง section แบบลายธงตาหมากรุกบางๆ พื้นผิว carbon fiber จางๆ บน hero
  และขีดเฉียงสีแดงเล็กๆ หน้าหมายเลข section (01/02/03)
- **Hero section** — เพิ่มเฉพาะหน้า Model Report ที่เป็นหน้าแรกเข้าเว็บ (ไม่ใส่ทุกหน้า
  เพื่อไม่ให้เว็บกลับไปดูเหมือน "marketing dashboard" ที่เคยถูกปรับออกไปแล้ว) มีหัวข้อ
  ตัวหนา Condensed แถบข้อมูลแบบ timing screen ที่เป็นข้อมูลจริง (MAE/RMSE/deviation/
  จำนวน lap ที่ประเมิน) และปุ่ม CTA ที่ลิงก์ไปหน้าใช้งานจริง ไม่มี countdown ปลอมๆ
- **Motion**: ตัวเลขสำคัญ (อันดับ, เวลาที่เร็วขึ้น, win probability, ตัวเลขสรุปต่างๆ)
  มี animation นับขึ้นตอนโหลดหน้า และแถวในตารางมี stagger reveal แบบเร็วๆ ที่จำกัดเวลา
  ไว้ไม่ให้ยืดยาด ทั้งหมดนี้เคารพ `prefers-reduced-motion` (มีการบังคับใช้ทั่วทั้งเว็บอยู่แล้ว
  ใน `base.html`)
- ไม่ได้เพิ่มไลบรารี GSAP หรือ animation library ใดๆ — ทุกอย่างเป็น CSS/vanilla JS ล้วน
  สอดคล้องกับแนวทางเดิมของโปรเจกต์ที่เขียน CSS เองไม่ใช้ framework

## 4. เพิ่มปัจจัยการแข่งขันจริง (ไม่ใช่ข้อมูลสมมติ)

- **สภาพอากาศจริง** — อุณหภูมิอากาศ อุณหภูมิแทร็ก ความชื้น และฝนตก/แห้ง ดึงจาก
  ข้อมูลสภาพอากาศจริงของ FastF1 ต่อสนาม แสดงในหน้า Analysis, Strategy Lab และเป็นคอลัมน์
  Wet/Dry ในตาราง Model Report (ซึ่งตอนนี้อธิบายได้แล้วว่าทำไมบางแถวของ Japanese GP
  ถึงมี deviation สูงผิดปกติ — เพราะเป็นสนามที่ฝนตกจริง)
- **ตำแหน่งกริดสตาร์ทจริง** — feature `Position` ของโมเดลเดิม hardcode เป็น `1`
  ให้ทุกคนที่จำลองไม่ว่าจะเป็นใคร ตอนนี้ใช้ตำแหน่งกริดจริงของแต่ละคน (เช่น VER
  ออกสตาร์ท P3 จริงในสนาม Australian GP 2025)
- **เวลาที่เสียตอนเข้าพิทจริงต่อสนาม** — เดิมใช้ค่าคงที่ 22.0 วินาทีทุกสนาม ตอนนี้ประมาณ
  จากข้อมูลจริงต่อสนาม โดยเทียบเวลา in-lap กับ lap ปกติจาก lap cache ที่มีอยู่แล้ว
  (สนามที่ทดสอบได้ค่า 14.7 วินาที) จะใช้ค่าคงที่ 22.0 เป็น fallback เฉพาะตอนที่ยังไม่มี
  ข้อมูล lap ของสนามนั้นเท่านั้น
- **สถานะ Safety Car / VSC / Red Flag จริง** — แสดงเป็นข้อมูลประกอบคู่กับสภาพอากาศ
  ดึงจากข้อมูล track status จริงของ FastF1
- **สิ่งที่ตั้งใจไม่เพิ่ม**: การเลือกยาง wet, การจำลองความช้าลงของ lap ตอนมี Safety Car
  หรือปัจจัยอื่นที่ต้อง "เทรนโมเดลใหม่" ถึงจะใช้งานได้จริง เพราะโมเดลปัจจุบันไม่เคยเทรน
  ด้วยข้อมูลพวกนี้ ถ้าใส่เข้าไปในตัวจำลองเลยจะได้ผลลัพธ์ที่ไม่มีความหมายโดยไม่มีใครรู้
  ซึ่งเป็นปัญหาแบบเดียวกับ "fake feature" ที่เซสชันนี้พยายามแก้อยู่ทุกจุด เรื่องนี้ต้องรัน
  `train_model_advanced.py` เทรนใหม่ ซึ่งเป็นงานแยกต่างหากที่ใหญ่กว่านี้

---

## การตรวจสอบ

ทุกอย่างข้างต้นทดสอบด้วยการรัน Flask app จริง (ไม่ใช่แค่อ่านโค้ด) — ยิง HTTP request ตรง
สำหรับ 1-stop/2-stop/Analysis/Model Report และตรวจสอบผ่านเบราว์เซอร์จริงด้วย Claude in Chrome
สำหรับส่วนดีไซน์ใหม่ (การแสดงผล hero, กรอบ focus ตอนกด Tab, ตัวเลข count-up ที่นับไปหยุดค่าที่ถูกต้อง,
stagger ของตาราง และการนำทางข้ามหน้า)

## ไฟล์ที่แก้ไข

```
app.py
data_pipeline.py
race_simulator.py
strategy_optimizer.py
templates/base.html
templates/index.html
templates/analysis.html
templates/play_strategy.html
```

## สิ่งที่อาจทำต่อ (ยังไม่ได้เริ่ม)

- เทรนโมเดลใหม่ (`train_model_advanced.py`) โดยรวมข้อมูล lap ตอนฝนตกและตอนมี Safety Car
  เข้าไปด้วย เพื่อให้เปิดใช้กลยุทธ์ยาง wet และการจำลองที่รู้จัก Safety Car ได้อย่างถูกต้องจริงๆ
- ยังไม่ได้ตรวจสอบการแสดงผลบนหน้าจอมือถือ (mobile viewport) แบบดูจริงในเบราว์เซอร์ครบถ้วน
  ในเซสชันนี้ (เครื่องมือ resize หน้าจอไม่ได้ผลตอน screenshot) — CSS ใหม่ใช้ `clamp()`/
  `flex-wrap` และ breakpoint เดิมของเว็บทั้งหมด แต่ยังไม่ได้ดูด้วยตาบนหน้าจอแคบจริงๆ
